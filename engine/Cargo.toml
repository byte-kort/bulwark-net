[package]
name = "engine"
version = "0.1.0"
edition = "2024"

[dependencies]
nix = { version = "0.31.3", features = ["socket", "net"] }
pnet = "0.35.0"
serde = { version = "1.0.229", features = ["derive"]}
serde_json = "1.0.151"
